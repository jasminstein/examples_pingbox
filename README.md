## Requirements

- Python >= 3.10
- numpy (version)
- pydsm (version)
- scipy (version)

## Variables

- f = frequency
- sound = arary/list corresponding to sound
- fs = sample rate
- phon = loudness in phons
- spl = sound pressure level (in db)